# Frontend

Planned React/TypeScript/Vite client:

- Form for city, date, event format, category, budget, duration, and language.
- Cards preserving the server order.
- Explicit success, category-absent, no-eligible, validation, loading, and
  network-error states.

Frontend implementation belongs to the spectra workstream.
