# Real API matching is the default, including when this variable is unset.
VITE_API_MODE=api

# The Vite dev server proxies /api to http://127.0.0.1:8000.
# For an explicitly labeled design preview only, set VITE_API_MODE=demo.
# API errors never switch the application to demonstration data.
