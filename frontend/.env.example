# Use local, explicit fixtures while the FastAPI server is not yet integrated.
VITE_API_MODE=demo

# After backend integration, remove the line above or set VITE_API_MODE=api.
# The Vite dev server proxies /api to http://127.0.0.1:8000.
